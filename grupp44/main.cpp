
#include <iostream>
#include <SDL.h>
using namespace std;

int main(int argc, const char * argv[]) {
    // insert code here...
    SDL_Init(SDL_INIT_EVERYTHING);
    
    cout << SDL_Init(SDL_INIT_EVERYTHING);
    cout << "Test Emma";

    return 0;
}
